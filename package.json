{
  "name": "blinds_hap",
  "version": "1.0.0",
  "description": "HAP.js frontend for my rust-based blinds_controller project",
  "main": "src/index.ts",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start.dev": "ts-node --project tsconfig.json src/index.ts",
    "start": "npm run build && node dist/index.js",
    "build": "tsc -p . --outDir dist --inlineSourceMap",
    "clean": "rm -rf node_modules dist"
  },
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@types/js-yaml": "^4.0.9",
    "@types/node": "^20",
    "ts-node": "^10.9.2",
    "typescript": "^5.7.3"
  },
  "dependencies": {
    "hap-nodejs": "^1.1.0",
    "js-yaml": "^4.1.0",
    "serialport": "^13.0.0"
  }
}
